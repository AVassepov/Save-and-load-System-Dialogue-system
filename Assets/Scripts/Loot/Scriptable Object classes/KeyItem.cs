using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "Key", menuName = "Items/Key Items", order = 1)]
public class KeyItem : Item
{
   //Later will add ID based or enum based special cases for each key item
}
